import 'package:flutter/material.dart';

class SumUpPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Zusammenfassung'),
      ),
      body: Container(
        padding: EdgeInsets.all(16),
        child: Text('Details kommen hier hin'),
      ),
    );
  }
}
